int teste(int z) {
	int g = z + 7;
	return g;
}

int compute_benchmark_raw(float* arrX, float* arrY, float* arrOut, 
	int size) 
{
	int x = 25;
    int y = 25;
	int val = 1;
	int old_x = x;

	for(int i = 0; i < 9; i = i + 1) {
		int yes = 25;
	}

	x = (x * x) - (y * y) + val;

	y = (20 * old_x * y) + val;
            
    val = x * 1 + y * 2;

	return 0;
}


int justcompile(int b) {
	int a = teste(3);
	while (a < 30) {
		a = a + b;
		if(a == 1) {
			return 0;
		} else if (a == 2) {
			return 1;
		} else {
			return a;
		}
		return 3;
	}

	if(a == 1) {
		return 0;	
	} else if (a == 2){
		return 1;
	} else {
		return 2;
	}

	return a;
}

int main() {
	int b = justcompile(5);
	int* test = &b;
	char* a = "asdasd";

	for(int i = 0; i < 30; i = i + 1) {
		b = b + i;
	}

	return b;	
}
